package interfaceEx;

public interface ICommon extends IOne{     //interface extends by another extends...if both(class-class)or (interface-interface) are same extends..if both are different it implements

}
